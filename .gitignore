# Rust build artifacts
/target

# Scratch / dogfooding — not part of the Vyer project
/playground
/results

# Local agent / MCP config (machine-specific absolute paths) + audit log
.mcp.json
.vyer-audit.log
.scry-audit.log

# OS / editor noise
.DS_Store
*.swp
*~
*.log
